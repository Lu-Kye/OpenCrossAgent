{
  "name": "opencrossagent",
  "version": "0.1.0",
  "private": true,
  "description": "Cross-agent orchestration gateway with multi-channel support",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "build": "pnpm -r build",
    "dev": "tsx scripts/dev.ts",
    "test": "pnpm -r test",
    "typecheck": "pnpm -r typecheck"
  },
  "devDependencies": {
    "tsx": "^4.21.0",
    "vitest": "^4.1.5"
  }
}
